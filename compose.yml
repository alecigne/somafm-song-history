services:
  db:
    image: docker.io/postgres:latest
    container_name: somafm-song-history-db
    restart: always
    environment:
      POSTGRES_USER: somafm
      POSTGRES_PASSWORD: password
      POSTGRES_DB: somafm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
